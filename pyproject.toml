[project]
name = "daily-digest"
version = "0.1.0"
description = "每日资讯摘要推送系统"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.32",
    "beautifulsoup4>=4.12",
    "pyyaml>=6.0",
    "openai>=1.0",
    "lxml>=5.0",
]
